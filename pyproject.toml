[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ankur8289-calculator"
version = "0.1"
description = "A command line tool that accepts parameters"
requires-python = ">=3.8"

[project.scripts]
adder = "calculator.tool:main"